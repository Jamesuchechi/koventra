import { buildMetadata } from '@/lib/seo';

export const metadata = buildMetadata({
  title: 'Contact',
  description: 'Contact Koventra Systems for venture inquiries, careers, press, and partnership opportunities.',
  pathname: '/contact',
});
